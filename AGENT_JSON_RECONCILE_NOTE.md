Merged `agent.json` and `.well-known/agent.json` into one canonical card based on the newer `.well-known` content, preserving the Cross-Standard dataset identity and adding the root-only `crossings`, `keyEssays`, and `blogIndex` fields.

Open human decision: confirm primary brand: 'Asaptic' vs 'Asaptic Labs' (currently name=Asaptic, alternateName=Asaptic Labs, legalName=Asaptic (HK) Limited).
