{
  "_comment": "SYNTHETIC TEST FIXTURE — mirrors the real denied-access body byte-shape emitted by GET /api/v1/spec-coded/{at_id}. Two jobs: (1) document the exact keys a client must branch on, (2) prove the feed schema rejects any non-feed document. Not a feed envelope, so it must never validate against asaptic.tender.v1.",
  "error": {
    "code": "SPEC_CODED_GATED",
    "message": "SYNTHETIC TEST FIXTURE. Spec-coded technical packages are available to approved accounts only. Request access with the request_tender_access tool on https://asaptic.com/mcp, or email engage@asaptic.com."
  },
  "at_id": "AT-TEST-HK-0000-001",
  "access_url": "https://portal.asaptic.com/"
}
