I'm going to start by listing the contents of the workspace directory to locate the files and directories mentioned in the request.
I'm listing the contents of the `standard/` directory to see where the template partials, `llms.txt`, and built pages are located.
I'm going to view the file `standard/templates/partials/jsonld.mjs` to assess the structured data structure and check the `@graph` completeness.
I will view the first 100 lines of `standard/llms.txt` to assess if it is well-formed and if its count header is current.
I will search for `application/ld+json` across the built HTML files in `standard/` to see what is generated for these pages.
I will view the portion of `standard/bess-china-to-algeria.html` containing the JSON-LD payload to analyze the actual output.
I will view lines 121 to 180 of `standard/bess-china-to-algeria.html` to see the rest of the JSON-LD structure.
I will view the `standard/templates/partials/i18n.mjs` file to check how URLs are generated and whether they match the actual static `.html` files.
I will search for references to `bess-china-to-algeria` in the codebase to see how internal links are formatted (with or without `.html`).
I will view `/Users/tun/Projects/asaptic-web/_worker.js` to see how incoming requests are mapped (e.g. if clean URLs like `/standard/bess-china-to-algeria` are mapped to `/standard/bess-china-to-algeria.html`).
I will list the `/Users/tun/Projects/asaptic-web/zh` directory to check how localized paths are structured in the static output.
I will list the `standard/exports/` directory to verify if the raw JSON distribution files exist and match the URL path.
I will search for the Question names in `standard/cosmetics-china-to-us.html` to see how its auto-generated FAQs look.
I will view the FAQ portion of `standard/cosmetics-china-to-us.html` to examine the questions generated for US cosmetics.
I will view `/Users/tun/Projects/asaptic-web/llms.txt` to see what is in the root `llms.txt`.
I will view the first 100 lines of `standard/llms-full.txt` to see if it acts as the comprehensive index or full-text representation of the compliance comparisons.
### GEO/AIO Audit Findings

Here is the assessment of the compliance-comparison site's citeability for AI search engines:

1. **JSON-LD `@graph` Completeness**: The nodes (`Dataset`, `TechArticle`, `FAQPage`, and `BreadcrumbList`) are all present. However, they exist as **loose/unconnected nodes** (there is no `WebPage` or `WebSite` entity to anchor the graph, and linking a `TechArticle` as `isPartOf` a `Dataset` is semantically invalid).
2. **Dataset distribution**: **Yes**, the `distribution` field correctly features a `DataDownload` block pointing to the raw JSON export (e.g., `exports/<slug>.json`).
3. **FAQ Phrasing**: **No**, the questions are awkward. They are derived by simply appending a `?` to the topic names, resulting in robotic phrasing like `"FDA Facility Registration — MoCRA Mandatory?"`.
4. **`llms.txt` Analysis**: Well-formed and the count header (`393 live comparisons`) matches the actual number of files. However, **all links in the index redirect to the generic `/standard/browse` page**, which hinders search engines from crawling the individual comparison URLs.
5. **High-ROI Additions**: Unify the graph with a `WebPage` entity, and add `about` properties (with Wikipedia links for standards like IEC 62619, CE, UL) to improve semantic matching.

---

### TOP 3 Concrete Improvements

#### 1. Map Individual Page Links in the LLM Index
*   **File**: [standard/llms.txt](file:///Users/tun/Projects/asaptic-web/standard/llms.txt)
*   **Change**: Replace the generic `https://asaptic.com/standard/browse` references with the specific HTML path structure, or add a prominent direction pointing LLMs to the fully detailed [standard/llms-full.txt](file:///Users/tun/Projects/asaptic-web/standard/llms-full.txt) to allow individual page discovery.
    ```diff
    - - Air-source heat pump (China → AUSTRALIA, CANADA, EU, INDIA, JAPAN, MEXICO, UK, US): https://asaptic.com/standard/browse
    + - Air-source heat pump:
    +   - Australia: https://asaptic.com/standard/heat-pump-china-to-australia
    +   - Canada: https://asaptic.com/standard/heat-pump-china-to-canada
    ```

#### 2. Clean Up Auto-Generated FAQ Phrasing
*   **File**: [standard/templates/partials/jsonld.mjs](file:///Users/tun/Projects/asaptic-web/standard/templates/partials/jsonld.mjs#L19-L30)
*   **Change**: Improve the fallback FAQ generation logic to cleanly transform topic strings (especially those containing `—`) into natural questions instead of appending a question mark.
    ```javascript
    // Replace the simple topic mapping with a cleaner question-formatting helper:
    const formatQuestion = (topic) => {
      let q = topic.split(" — ")[0].trim();
      if (!q.toLowerCase().startsWith("what") && !q.toLowerCase().startsWith("is")) {
        return `What are the requirements for ${q}?`;
      }
      return q.endsWith("?") ? q : `${q}?`;
    };
    ```

#### 3. Anchor the Schema Graph using a `WebPage` Node
*   **File**: [standard/templates/partials/jsonld.mjs](file:///Users/tun/Projects/asaptic-web/standard/templates/partials/jsonld.mjs#L31-L83)
*   **Change**: Insert a `WebPage` node in the `@graph` array and update the `isPartOf` link of `TechArticle` and `Dataset` to point to it, unifying the graph structure:
    ```javascript
    {
      "@type": "WebPage",
      "@id": url,
      "url": url,
      "name": t(data.page.title, lang),
      "isPartOf": { "@id": `${site}/#website` },
      "breadcrumb": { "@id": `${url}#breadcrumb` }
    }
    ```

---

### Rest Optional

*   **Semantic Entity Linking (`about`)**: In [standard/templates/partials/jsonld.mjs](file:///Users/tun/Projects/asaptic-web/standard/templates/partials/jsonld.mjs#L31-L60), inject Wikipedia/Wikidata entities in an `about` property for `Dataset` or `TechArticle` pointing to standard subjects (e.g., [IEC 62619](https://en.wikipedia.org/wiki/IEC_62619) or [CE marking](https://en.wikipedia.org/wiki/CE_marking)).
*   **Link Publisher via `sameAs`**: Add `sameAs` references to the publisher organization node mapping back to official company social profiles or crunchbase profiles.
*   **Citations**: Link the `citation` property in `TechArticle` directly to target regulators (e.g., CREG, Sonelgaz, IANOR).
AGYEXIT=0
